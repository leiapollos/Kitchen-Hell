using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// List of tasks
/// </summary>
public class Tasks : Request
{


    // Start is called before the first frame update
    public Tasks() : base()
    {
        Name = "TASK";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
